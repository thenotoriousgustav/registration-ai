export default function DetailApplicant({
  params,
}: {
  params: { id: string };
}) {
  const { id } = params;

  return (
    <div>
      <h1>name : ucup</h1>
      <h1>user id : 2</h1>
      <h1>list evidence : </h1>
    </div>
  );
}
